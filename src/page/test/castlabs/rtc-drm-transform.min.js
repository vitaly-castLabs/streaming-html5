'use strict'

const rtcDrmGetVersion = () => {
    return '2.0.0-aabbccd';
}

const rtcDrmConfigure = (drmConfig) => {
}

const rtcDrmOnTrack = (event) => {
}

class rtcDrmEnvironments {
    static Development = new rtcDrmEnvironments("development");
    static Staging = new rtcDrmEnvironments("staging");
    static Production = new rtcDrmEnvironments("production");

    #name

    constructor(name) {
        this.#name = name;
    }

    toString() {
        return `${this.#name}`;
    }
}

export {rtcDrmGetVersion, rtcDrmConfigure, rtcDrmOnTrack, rtcDrmEnvironments};
